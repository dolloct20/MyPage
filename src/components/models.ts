export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Patient {
  id: string;
  name: string;
  birth: string;
  age: number;
  phone: string;
  address: string;
  chr_no: string;
  sex: number;
}